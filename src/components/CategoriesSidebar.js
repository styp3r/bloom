export default function CategoriesSidebar() {
    return (
        <div id = "categoriesSidebar">
            <div id="categories">
                {/*Add Link tags from react-router-dom for each category*/}
            </div>

            <div id="sidebarFooter"></div>
        </div>
    );
}