import Categories from './CategoriesSidebar'
import HeroMain from './HeroMain'

export default function Hero(){
    return (
        <div id="hero">
            <Categories />
            <HeroMain />
        </div>
    ); 
}